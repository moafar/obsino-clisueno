entrada:
  ruta: "../../staging"

procesamiento:
  tipos_validos: ["docx", "doc", "rtf"]
  glob_patron: "**/*"          # opcional
  barra_progreso: true         # opcional

salida:
  carpeta_csv: "../../staging"
  # archivo_unificado: "unificado.csv" # <- Candidato a desaparecer porque en el nuevo main ya no se unificarán todos los CSVs

logging:
  level: "INFO"                # DEBUG/INFO/WARNING/ERROR/CRITICAL
  dir: "/home/rom/obsino/clisueno/logs"
